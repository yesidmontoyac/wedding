name: Deploy Wedding Invitation

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          VITE_SCRIPT_URL:        ${{ secrets.VITE_SCRIPT_URL }}
          VITE_DRIVE_FOTO1_ID:    ${{ secrets.VITE_DRIVE_FOTO1_ID }}
          VITE_DRIVE_FOTO2_ID:    ${{ secrets.VITE_DRIVE_FOTO2_ID }}
          VITE_DRIVE_FOTO3_ID:    ${{ secrets.VITE_DRIVE_FOTO3_ID }}
          VITE_DRIVE_ENTRADA1_ID: ${{ secrets.VITE_DRIVE_ENTRADA1_ID }}
          VITE_DRIVE_ENTRADA2_ID: ${{ secrets.VITE_DRIVE_ENTRADA2_ID }}
          VITE_DRIVE_PLATO1_ID:   ${{ secrets.VITE_DRIVE_PLATO1_ID }}
          VITE_DRIVE_PLATO2_ID:   ${{ secrets.VITE_DRIVE_PLATO2_ID }}
          VITE_DRIVE_VIDEO1_ID:   ${{ secrets.VITE_DRIVE_VIDEO1_ID }}
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
